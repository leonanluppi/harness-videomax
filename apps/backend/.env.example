PORT=4000
LOG_LEVEL=info
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/videomax_main
# Optional — defaults shown. NODE_ENV=production adds `Secure` to the session cookie.
NODE_ENV=development
SESSION_TTL_DAYS=30
