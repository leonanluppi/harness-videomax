---
description: Drive an entire PRD wave in parallel via worktrees + tmux. See skill for input grammar.
---

Invoke the `implement-and-evaluate-tmux` skill with `$ARGUMENTS` as the input string. The skill owns input parsing, validation, dispatch, polling, and reporting — refer to its SKILL.md for behavior.

User input: $ARGUMENTS
